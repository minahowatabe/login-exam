<h1>Blog Lists</h1>

<p><%= notice %></p>

<table>
  <tr>
    <th>Title</th>
    <th>Content</th>
  </tr>

<% @blogs.each do |blog| %>
  <tr>
    <td><%= blog.title %></td>
    <td><%= blog.content %></td>
    
    <td><%= link_to 'check the blog', blog_path(blog.id) %></td>
    <td><%= link_to "edit the blog", edit_blog_path(blog.id) %></td>
    <td><%= link_to 'delete the blog', blog_path(blog.id), method: :delete, data: { confirm: 'Are you sure you want to delete this blog?' } %></td>

  </tr>
<% end %>
</table>

<br>

<%= link_to 'Create New blog', new_blog_path %>